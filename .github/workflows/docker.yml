name: Docker Image

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/jvz-devx/termkit

jobs:
  build:
    name: Build and publish
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags
        id: meta
        shell: bash
        run: |
          short_sha="${GITHUB_SHA::12}"
          build_date="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          package_version="$(node -p "require('./package.json').version")"
          {
            echo "short_sha=${short_sha}"
            echo "build_date=${build_date}"
            echo "package_version=${package_version}"
            echo "tags<<EOF"
            echo "${IMAGE_NAME}:${short_sha}"
            echo "${IMAGE_NAME}:main"
            echo "${IMAGE_NAME}:${package_version}"
            echo "${IMAGE_NAME}:latest"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          build-args: |
            TERMKIT_COMMIT_SHA=${{ github.sha }}
            TERMKIT_SHORT_SHA=${{ steps.meta.outputs.short_sha }}
            TERMKIT_BUILD_DATE=${{ steps.meta.outputs.build_date }}
            TERMKIT_PACKAGE_VERSION=${{ steps.meta.outputs.package_version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
